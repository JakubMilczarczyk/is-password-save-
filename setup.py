from setuptools import setup

setup(
    name='password_validators',
    version='0.1',
    description='collection of password validators',
    author='Snow_Juzek',
    packages=['password_validators'],
    install_requires=['requests']
)

# in Terminal: python setup.py sdist
